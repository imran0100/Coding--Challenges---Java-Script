let nums = [15, 6, 5, 7];
var differenceOfSum = function (nums) {
  // digits = nums.join(" ");
  console.log(nums.join(""));
  for (var i = 0; i < digits.length; i++) {
    digits[i] = +digits[i];
  }
  console.log(digits);
};
differenceOfSum();
